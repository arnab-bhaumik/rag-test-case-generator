[pytest]
testpaths = tests
# All tests here are function-based — disabling class collection avoids a
# spurious warning every time a test file imports the models.TestCase
# Pydantic model (pytest's default `Test*` pattern treats it as a test class).
python_classes =
